def area(a, h):
    # takes the base length a and the height of a tringle, returns area of that triangle
    return a * h / 2

def perimeter(a, b, c):
    # takes the sides of a triangle a, b, c, returns perimeter of that triangle
    return a + b + c